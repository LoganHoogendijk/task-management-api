# Copy this to .env if running outside Docker.
# When using `docker compose up`, these are not needed — compose sets DATABASE_URL itself.
DATABASE_URL=postgresql+asyncpg://taskmanager_user:devpassword@localhost:5433/taskmanager
TEST_DATABASE_URL=postgresql+asyncpg://taskmanager_user:devpassword@localhost:5433/taskmanager_test